server:
  name: ag_stress_b

kitex:
  server:
    Port: 9201
    AdaptivePort: true
    ServiceName: ${server.name}_grpc
    EnableIPRange: 0:255
    Grpc:
      enable: true
      MaxConnectionIdle: 10
  client:
    Resolver:
      Enable: true
    Conn:
      GRPCConnPoolSize: 1

hertz:
  server:
    port: 9251
    adaptivePort: true
    serviceName: ${server.name}_http

nacos:
  config:
    serveraddr: 127.0.0.1:8813
    namespace: test
    username: nacos
    password: '{cipher}bmFjb3M2'
    # dataids:
    #   - dataid: test
    #     group: test
    #     type: yaml
  naming:
    serveraddr: ${nacos.config.serveraddr}
    namespace: ${nacos.config.namespace}
    username: ${nacos.config.username}
    password: ${nacos.config.password}
    
data:
  db:
    user:
      driver: mysql
      dsn: "root:root@tcp(localhost:3306)/houzw?parseTime=True"    

# agredis:
#   config:
#     addrs:
#       - "127.0.0.1:6379"
#     MaxRetries: 1
#     ReadTimeout: 100

aglog:
  topHandler: [zap1]
  zap:
    logs:
      zap1:
        log_level: debug
        encoding: console           # json or console
        compress: true
        console: true
        stdout: true
